# ===================== Seata 统一配置（Server + Client 合并，供 Nacos 下发）=====================
# Nacos: dataId=seataServer.properties  group=SEATA_GROUP
# 由 deploy/seata/push-config.sh 在 compose 启动时自动推送到 Nacos。
# __MYSQL_HOST__ / __MYSQL_PORT__ / __MYSQL_PASSWORD__ 占位符会被 push-config.sh 用运行时变量替换。
# Seata 2.0 采用「统一单 dataId」：Server 读取 store.*/server.*/transport.*，Client 读取 service.*/client.*。

# ------------------------- 存储模式：DB（生产推荐）-------------------------
store.mode=db
lock.mode=db

store.db.datasource=druid
store.db.dbType=mysql
store.db.driverClassName=com.mysql.cj.jdbc.Driver
store.db.url=jdbc:mysql://__MYSQL_HOST__:__MYSQL_PORT__/seata?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
store.db.user=root
store.db.password=__MYSQL_PASSWORD__
store.db.minConn=5
store.db.maxConn=100
store.db.globalTable=global_table
store.db.branchTable=branch_table
store.db.distributedLockTable=distributed_lock
store.db.lockTable=lock_table
store.db.queryLimit=100

# ------------------------- 事务日志 / 超时 / 重试 -------------------------
server.undo.log.save.days=7
server.undo.log.delete.period=86400000
server.default-global-transaction-timeout=60000
server.commit.retry.count=5
server.rollback.retry.count=5

# ------------------------- 日志 -------------------------
log.exceptionRate=100

# ------------------------- 传输（transport）-------------------------
transport.type=TCP
transport.server=NIO
transport.heartbeat=true
transport.enableClientBatchSendRequest=true
transport.threadFactory.bossThreadPrefix=NettyBoss
transport.threadFactory.workerThreadPrefix=NettyServerNIOWorker
transport.threadFactory.serverExecutorThreadPrefix=NettyServerBizHandler
transport.threadFactory.shareBossWorker=false
transport.threadFactory.clientSelectorThreadPrefix=NettyClientSelector
transport.threadFactory.clientSelectorThreadSize=1
transport.threadFactory.clientWorkerThreadPrefix=NettyClientWorkerThread
transport.threadFactory.bossThreadSize=1
transport.threadFactory.workerThreadSize=default
transport.shutdown.wait=3

# ------------------------- 事务分组映射（Server + Client 共用）-------------------------
service.vgroupMapping.my_test_tx_group=default
service.enableDegrade=false
service.disableGlobalTransaction=false

# ------------------------- 客户端 RM -------------------------
client.rm.asyncCommitBufferLimit=1000
client.rm.reportRetryCount=5
client.rm.tableMetaCheckEnable=true
client.rm.reportSuccessEnable=false
client.rm.sagaBranchRegisterEnable=false
client.rm.sagaJsonParser=fastjson
client.rm.sqlParserType=druid

# ------------------------- 客户端 TM -------------------------
client.tm.commitRetryCount=5
client.tm.rollbackRetryCount=5
client.tm.defaultGlobalTransactionTimeout=60000
client.tm.degradeCheck=false
client.tm.degradeCheckAllowTimes=10
client.tm.degradeCheckPeriod=2000

# ------------------------- 客户端 Undo -------------------------
client.undo.dataValidation=true
client.undo.logSerialization=jackson
client.undo.logSaveDays=7
client.undo.logDeletePeriod=86400000
client.undo.logTable=undo_log

# ------------------------- 客户端日志 -------------------------
client.log.exceptionRate=100
